# Copy this to .env.local for local dev (npm run dev)
# Points the frontend directly at the locally-running backend.
VITE_API_BASE_URL=http://localhost:8000/api/hakolect/v1

# Optional: set your API key if you enabled it in the backend .env
# VITE_API_KEY=your-api-key-here
